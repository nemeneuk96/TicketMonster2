como iniciar en git hub

git config --global user.name "carlos"
git config --global user.email "towa_carlos@hotmail.com"
creas un repositorio en github
luego click derecho git gui

rescan
stage changed
commit al costado escribir 
commit
push
